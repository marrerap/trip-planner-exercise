<!DOCTYPE html>
<html lang="en">
  <head>
    <h1><%= title %> </h1>
  </head>
  <body>
   <title><%= title %> </title>
   <a href="/new">Add New Trip</a>   
   <p><% db.forEach((trip) => { %>
    <h2><%= trip.title %> </h2>    
    <a href="/new/<%= trip.id %> ">View Item</a>
    <form method="POST" action="/delete/<%= trip.id %>">
      <button type="submit" formaction="/delete/<%= trip.id %>">remove</button>
    </form>    
<% }); %></p>
    
    
    
  </body>
</html>